import React from "react";
import classNames from "classnames";

// import "./xxx.scss";

const xxx = ({ prop, className }) => {
  return <div className={classNames(["xxx", className])}></div>;
};

export default xxx;
